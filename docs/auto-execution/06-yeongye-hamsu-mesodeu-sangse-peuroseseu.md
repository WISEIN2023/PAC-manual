---
id: auto-execution/06-yeongye-hamsu-mesodeu-sangse-peuroseseu
doc: auto-execution
title: 6. 연계 함수 · 메소드 상세 (프로세스 관점)
parent: docs/auto-execution/README.md
---

# 6. 연계 함수 · 메소드 상세 (프로세스 관점)

각 오브젝트를 '언제 누가 호출하고(호출관계), 무엇을 입력받아, 어떤 처리를 하고, 어떤 테이블을 참조/갱신하며, 어떤 예외가 있는지' 관점으로 정리.

| 구분 | 오브젝트 | FG/Class | 호출관계 (선행 ← / → 후행) | 주요 입력 | 처리·효과 (프로세스 관점) | 참조/갱신 테이블 | 예외·오류 |
|---|---|---|---|---|---|---|---|
| 함수 | ZFPAC_CREATE_BATCHJOB | ZPAC050 | ← 모든 CREATE_*_JOB, SAIL_PROCESS_ID / → JOB_OPEN·SUBMIT·JOB_CLOSE | JOBNAME, REPNAME(ZLPAC0100/0101), 파라미터 SELTAB, PID | PAC 배치잡 생성의 단일 관문. 밸런싱 점검 후, 동일 JOBNAME이 R/S/Y로 실행 중이면 중복 생성 방지. JOB_OPEN→SUBMIT(대상 프로그램) VIA JOB→JOB_CLOSE로 배치잡을 등록하며 즉시/예약 실행을 분기한다. | TBTCO, TBTCP | JOB_ERROR·NO_FOUND. EV_RC_START=9(중복 실행중) |
| 함수 | ZFPAC_CHECK_JOB_BALANCING | ZPAC290 | ← CREATE_BATCHJOB·GET_CAN_START·GLOBAL_GET_CAN_START | PID(레벨 판별용), WAIT | 서버별 배치 워크프로세스(BTP)의 유휴율을 계산하고 ZTPACSYS의 레벨별 허용율(RATE_PCSGP/ACT/PID)과 비교. 여유가 부족하면 WAIT_TIME만큼 대기 후 재시도(또는 미생성), 여유가 있으면 EV_CREATE='X'. | ZTPACSYS | 없음 (EV_CREATE 리턴) |
| 함수 | ZFPAC_USER_AUTH | ZPAC040 | ← SAIL_PROCESS_ID, CREATE_PCSGP_JOB | GPID/BUPAK/조직, PID, USRID | 실행 담당자·Posting User·메일 수신자 등 권한정보를 반환. Posting User는 우선순위(① Activity ID > ② Activity Group > ③ Organization)로 한 명 결정. HQ 권한자(CHECK_AUTH_HQ)는 전체 권한. | ZTPAC_CONFIG, 권한(AGR_*) | PARAM_ERROR, NOT_FOUND |
| 함수 | ZFPAC_GLOBAL_GET_CAN_START | ZPAC050 | ← CREATE_GPID_JOB, GET_CAN_START / → SELECT_NODE_EXECUTABLE | GPID, 조직, 년월 | Global Package 레벨 수행 가능 여부 판정. Main Business Package 마감 확인, GPID NODE·LINK 조회, 수행 가능 노드 계산 후 이미 실행 중인 배치잡은 제외하여 EV_CANSTART 반환. | ZTPAC_GPID, TBTCO/TBTCP | 없음 |
| 함수 | ZFPAC_GET_CAN_START | ZPAC050 | ← Fiori 조회, SAIL_GLOBAL_PROCESS / → CHECK_CAN_RUN | BUPAK/조직, PCSGP, GPID/MVID | Activity Group 단위 수행 가능 판정. 조직·기간 유효성→마감→상태 Sync→Rework 점검 후 CHECK_CAN_RUN으로 수행 가능 노드 목록 산출. Global 레벨이면 GLOBAL_GET_CAN_START로 위임. | ZTPAC_STATUS, ZTPAC_GPID | INPUT_REQUIRED |
| 함수 | ZFPAC_GET_RUNNING_JOB | ZPAC053 | ← CREATE_BUPAK/PCSGP_JOB, SAIL_PROCESS_GROUP | BUPAK/조직, PCSGP, PID, JOBTYP(R/S/T) | 현재 실행 중 배치잡 반환. Job 유형(R:Business Package/S:Activity Group/T:Transaction)별 JobName을 GET_BATCH_JOBNAMING으로 구성해 TBTCP·TBTCO에서 R/S/Y 상태 Job을 조회, PAC 상태(ZTPAC_STATUS)와 매핑. | TBTCP, TBTCO, ZTPAC_STATUS, ZTPAC_LOG_HDR | 없음 |
| 함수 | ZFPAC_CREATE_GPID_JOB | ZPAC050 | ← EXECUTE_ACTION(PCSGP_START) / → ZFPAC_CREATE_BATCHJOB | GPID, Company Code, 년월 | Global Package(GPID) 레벨 배치잡 생성. GLOBAL_GET_CAN_START로 시작 가능 확인, CHK_CLOSING_ALL로 마감 확인 후 ZLPAC0100을 배치잡으로 등록. JobName은 [PAC]GPID_년월_회사코드. | ZTPAC_GPID | NO_ROOT_TYPE, EV_SUBRC |
| 함수 | ZFPAC_CREATE_BUPAK_JOB | ZPAC050 | ← EXECUTE_ACTION(PCSGP_START) / → ZFPAC_CREATE_BATCHJOB | BUPAK/조직, 년월, START_FROM | Business Package(1레벨) 배치잡 생성. GET_RUNNING_JOB(R)로 이미 실행 중이면 종료, Precheck(XPRE_USE) 및 CHK_CLOSING_ALL 확인 후 ZLPAC0100을 배치잡으로 등록. | ZTPAC_CONFIG | NO_ROOT_TYPE, EV_SUBRC |
| 함수 | ZFPAC_CREATE_PCSGP_JOB | ZPAC050 | ← EXECUTE_ACTION, SAIL_GLOBAL_PROCESS, CREATE_PROCESS_GROUP_JOB / → SAIL_PROCESS_ID(S) | BUPAK/조직, PCSGP, HERE, START_FROM, CRSCODE | Activity Group/Sub-Group 배치잡 생성. 조직·기간 유효성, Precheck, CHK_CLOSING_ALL, 실행 중 Job 확인, (Start All 시)ZFPAC_CHECK_PRENODE로 선행 완료 확인 후 SAIL_PROCESS_ID(IV_TYPE=S)로 그룹 Job 생성. | ZTPAC_PROC, ZTPAC_CONFIG, ZTPAC_CROSS_IF | WRONG_PROCESS_GROUP, EV_SUBRC |
| 함수 | ZFPAC_CHECK_PRENODE | ZPAC051 | ← CREATE_PCSGP_JOB | BUPAK/조직, PCSGP, PID, HERE | Start All 수행 시 대상 Activity Group의 선행(같은 레벨) + 부모/조부모 Activity Group의 선행까지 모두 완료되었는지 재귀 확인. 미완료 시 EV_SUBRC=2로 수행 차단(ZPAC01/069,070). | ZTPAC_PROC, ZTPAC_STATUS, ZTPAC_GPID | EV_SUBRC=2 (미완료) |
| 함수 | ZFPAC_CONFIRM_ITEM | ZPAC052 | ← EXECUTE_ACTION(CONFIRM_ITEM), SAIL_PROCESS_GROUP(REPTY=C) / → MANUAL_CONFIRM/SCHEDULE_CLOSE_BY_PID, NEXT_AUTO_START | BUPAK/조직, PID, REASON | Activity 수기 확정. 조직·기간·Confirm 가능 여부·Final 완료·마감·Lock·실행 중 Job을 순차 점검한 뒤 확정. Trigger 유형이면 AUTOTRIG 호출, Closing이면 SCHEDULE_CLOSE_BY_PID. 확정 후 AFTER_CONF='X'면 NEXT_AUTO_START로 후행 자동수행. | ZTPAC_PROC, ZTPAC_STATUS(_IDV), ZTPAC_CONFIG, ZTPAC_CROSS_IF | 다수(062/296/655/101/042/265 등) |
| 함수 | ZFPAC_AUTOTRIG_CROSS_BUPAK / _ORG | ZPAC054 | ← SAIL_PROCESS_GROUP(BUPAK_TRIGGER), CONFIRM_ITEM / → START_FROM_AUTO_TRIGGER | BUPAK/조직, CRSCODE, MODE(E/C) | Out-bound 트리거. ZTPAC_CROSS_IF로 Outbound/Inbound PID를 찾아 조직 레벨을 매칭. Outbound 조직이 모두 완료되면 Inbound Activity를 확정하고, Inbound가 XAUTO면 START_FROM_AUTO_TRIGGER로 후속 자동수행 개시. XREWORK=' '면 완료건 재수행 차단. | ZTPAC_CROSS_IF, ZTPAC_PROC, ZTPAC_STATUS | REQUREIRED_MISSING, 077/078/171/285/477 |
| 함수 | ZFPAC_NEXT_AUTO_START | ZPAC05x | ← ZFPAC_CONFIRM_ITEM | BUPAK/조직, PCSGP, PID | 수기 확정 직후 후행 노드가 자동으로 이어지도록 자동수행을 개시(그룹/PID Job 생성 트리거). AFTER_CONF 설정 시에만 호출된다. | ZTPAC_CONFIG, ZTPAC_STATUS | ES_RETURN(E) |
| 함수 | ZFPAC_ORG_AUTH | ZPAC04x | ← EXECUTE_ACTION(AUTH_CHECK) | GPID/BUPAK/조직 | 조직 단위 조회·수행 권한 존재 여부(EV_AUTH_EXIST)를 반환. Fiori 진입 시 조직 권한 게이트 역할. | 권한(AGR_*), ZTPAC_CONFIG | 없음 |
| 함수 | ZFPAC_STOP_PCSGP_JOB / ZFPAC_CREATE_PID_JOB | ZPAC05x | ← EXECUTE_ACTION(PCSGP_STOP / PID_START) | BUPAK/조직, PCSGP / PID | STOP: 실행 중 그룹 배치잡을 중단(BP_JOB_ABORT 등). CREATE_PID_JOB: 단일 Activity(PID)만 개별 배치잡으로 실행(자동수행 전체가 아닌 단건 실행 경로). | TBTCO, ZTPAC_PROC | EV_SUBRC |
| 메소드 | ZCL_PAC → CHECK_BUPAK_CLOSE | ZCL_PAC | ← GET_CAN_START, CHK_CLOSING_ALL, EXECUTE_ACTION | BUPAK/조직, 년월 | Business Package 월마감 여부 판정. ZTPAC_CONFIG의 월마감 사용(Active monthly closing) 설정 시 ZTPAC_CLOSE에 마감월이 존재하면 마감으로 판정하고 마감월(EV_CLMON)을 반환. | ZTPAC_CLOSE, ZTPAC_CONFIG, ZTPAC_GPID | RV_CLOSED |
| 메소드 | ZCL_PAC_CLOSING → CHK_CLOSING_ALL | ZCL_PAC_CLOSING | ← 모든 CREATE_*_JOB, CONFIRM_ITEM, AUTOTRIG | BUPAK/조직, 년월, PCSGP, PID | 마감 판정은 4단계로 순차 수행되며 하나라도 마감이면 자동수행을 차단한다<br>① 표준 전기기간 마감(FI_PERIOD_CHECK) →<br>② Activity/Group에 연결된 Closing Schedule(ZFPAC_CLOSING_SCHID) →<br>③ Final Closing 일정(CHK_FINAL_CLOSING) →<br>④ Business Package 마감(CHECK_BUPAK_CLOSE, 테이블 ZTPAC_CLOSE).<br>※ 하나라도 마감이면 EV_CLOSED='X'<br>※ ④는 ZTPAC_CONFIG에서 'Active monthly closing by business package'가 설정된 경우에만 적용. | ZTPAC_SCH_*, ZTPAC_PROC_RCLOS, ZTPAC_CLOSE, T001B | ES_RETURN(E) |
| 메소드 | ZCL_PAC_CLOSING → CHK_ACT_CLOSED / CHK_FINAL_CLOSING | ZCL_PAC_CLOSING | ← CHK_CLOSING_ALL | BUPAK/조직, 년월, PCSGP/PID | CHK_ACT_CLOSED: Activity/Group에 연결된 Closing Schedule(ZFPAC_CLOSING_SCHID) 마감 확인. CHK_FINAL_CLOSING: Final 일정의 확정/일시 경과 여부로 마감 판정(STATIC 캐시로 반복조회 최적화). | ZTPAC_PROC_RCLOS, ZTPAC_SCH_PLAN/ID | EV_CLOSED |
| 메소드 | ZCL_PAC_SAIL → SAIL_START | ZCL_PAC_SAIL | ← ZLPAC0100 / → SAIL_GLOBAL/BUSINESS/PROCESS_GROUP | AS_LOG_ORG(레벨 정보) | 자동수행 엔진 진입. NODE/PID/LINK를 조회하고 레벨(GPID/BUPAK/그 외)로 분기한 뒤, 종료 시 CALL_APC로 화면 실시간 반영, START_REWORK_CHECK로 Rework 점검. | ZTPAC_PROC, LINK/NODE 뷰 | - |
| 메소드 | ZCL_PAC_SAIL → SAIL_PROCESS_GROUP ★ | ZCL_PAC_SAIL | ← SAIL_START / → SAIL_PROCESS_ID(T), CREATE_PROCESS_GROUP_JOB, ZFPAC_CONFIRM_ITEM, BUPAK_TRIGGER | AV_PCSGP, AV_START_FROM | 핵심 실행 로직. 노드를 DO~ENDDO로 순회하며 Rework·SYNC_LINK_INFO·SELECT_NODE_EXECUTABLE·선행완료(IS_PRE_PCSGP_COMPLETED) 점검. 수행 가능 노드가 없으면 실행 중 Job 대기(5초)·강제중단 Job 오류처리. 노드 유형별(Period Skip/Closing/Final/Dummy/Trigger/Sub-Group/프로그램) 분기 처리. | ZTPAC_STATUS, ZTPAC_LOG_HDR, TBTCO | INPUT_ERROR_LOG(각종) |
| 메소드 | ZCL_PAC_SAIL → SAIL_PROCESS_ID ★ | ZCL_PAC_SAIL | ← SAIL_PROCESS_GROUP/BUSINESS/GLOBAL / → ZFPAC_CREATE_BATCHJOB | IV_TYPE(S/T), IV_PID, IV_USER | 최하단(또는 그룹) Job 생성. SET_EXEC_PARAM으로 파라미터·Variant 세팅, GET_BATCH_JOBNAMING으로 JobName, ZFPAC_USER_AUTH로 Posting User, CHECK_AUTH_BY_PID로 권한 확인 후 ZFPAC_CREATE_BATCHJOB 호출. 실행 프로그램은 S=ZLPAC0100, T=ZLPAC0101. | ZTPAC_PROC, TSTC, TBTCO | ZPAC01/021(무권한), Job 상태 감시 |
| 메소드 | ZCL_PAC_SAIL → SET_EXEC_PARAM | ZCL_PAC_SAIL | ← SAIL_PROCESS_ID, ZLPAC0101 | IS_LOG_ORG, IV_PROG, 공통정보 | Activity Master(ZTPAC_PROC)에 등록된 Variant/파라미터와 공통 정보(조직·년월·PID)를 실행 대상 프로그램의 스크린 파라미터(RSPARAMS)로 매핑. 필수 공통 파라미터 누락은 ET_COM_ERR로 반환. | ZTPAC_PROC, TVARV/Variant | ET_COM_ERR |
| 메소드 | ZCL_PAC_SAIL → GET_BATCH_JOBNAMING | ZCL_PAC_SAIL | ← 다수(생성/조회 함수) | IV_JOB_TYPE, IV_TYPE(R/S/T), IS_LOG_ORG | 배치잡 명명 규칙 생성. [PAC] + 유형(G/S/B/공란) + 년(2)월 + 조직(레벨별) + PCSGP/PID. Rework는 접미사 _RW. Job 중복/조회의 키가 되는 표준 이름을 만든다. | - | - |
| 메소드 | ZCL_PAC_SAIL → SELECT_NODE_EXECUTABLE | ZCL_PAC_SAIL | ← SAIL_* 전체 | CT_NODE, CT_LINK | 각 노드의 수행 가능 여부 계산. 선행 노드가 모두 완료된 노드의 실행 플래그(EXEC='X')를 세팅. '선행완료→후행실행' 규칙의 판정 엔진. | - | - |
| 메소드 | ZCL_PAC_SAIL → SYNC_LINK_INFO / SYNC_GPID_STATUS | ZCL_PAC_SAIL / ZCL_PAC | ← SAIL_* 전체 | - | Activity 상태·LINK 연결 정보를 재조회(REFRESH_NODE_STATUS)하여 수행 가능 계산 시 실시간 데이터가 반영되도록 동기화. 자동수행 중 상태 변화를 즉시 반영하는 역할. | ZTPAC_STATUS, LINK 뷰 | - |
| 메소드 | ZCL_PAC_SAIL → START_REWORK_CHECK | ZCL_PAC_SAIL | ← SAIL_START/BUSINESS/PROCESS_GROUP | IV_PCSGP | Rework 발생 여부 점검. 강제 중단된 배치잡을 로그에 강제 반영(UPDATE_JOB_CANCEL_LOG)하고, Rework 발생 시 상태를 오류로 처리해 후행 실행을 차단. Rework 배치가 실행 중이면 종료. | ZTPAC_STATUS, TBTCO/TBTCP | - |
| 메소드 | ZCL_PAC_SAIL → IS_PRE_PCSGP_COMPLETED / CHECK_FINAL_COMPLETE / DUMMY_AUTO_CONFIRM | ZCL_PAC_SAIL | ← SAIL_PROCESS_GROUP | PID/PCSGP, 조직 | IS_PRE_PCSGP_COMPLETED: 상위·선행 Group 완료 확인. CHECK_FINAL_COMPLETE: Final Activity 종료 전 타 Activity 완료 확인. DUMMY_AUTO_CONFIRM: Dummy(REPTY=M) 노드 자동 확정. | ZTPAC_STATUS, ZTPAC_PROC | - |
| 메소드 | ZCL_PAC_SAIL → CREATE_PROCESS_GROUP_JOB / START_FROM_AUTO_TRIGGER | ZCL_PAC_SAIL | ← SAIL_PROCESS_GROUP / AUTOTRIG | PCSGP / PID, CRSCODE | CREATE_PROCESS_GROUP_JOB: 하위 Activity Sub-Group의 배치잡 생성(ZFPAC_CREATE_PCSGP_JOB 경유). START_FROM_AUTO_TRIGGER: 트리거 확정 후 Inbound Activity부터 후속 자동수행 개시. | ZTPAC_PROC | - |
| 메소드 | ZCL_PAC_AUTH → CHECK_AUTH_BY_PID | ZCL_PAC_AUTH | ← SAIL_PROCESS_ID | BUPAK/조직, PID, USER, TCODE | 최종 프로그램 실행 권한 판정. PAC Execute 권한 + (PID 레벨 실행 시)SAP 표준 트랜잭션 권한을 함께 확인. HQ 권한자·Extra Role를 반영하며, 무권한이면 EV_SUBRC로 오류(ZPAC01/021). | 권한(AGR_*), ZTPAC_CONFIG | EV_SUBRC=1/2 |
| 메소드 | ZCL_PAC_ORG → CHECK_VALID_ORG / CHECK_VALID_PERIOD | ZCL_PAC_ORG | ← 거의 모든 함수 진입부 | BUPAK, 조직, 년월 | 모든 함수의 공통 전처리. 조직 레벨(회사/BA/기타)에 맞게 조직값을 정규화·검증하고, 기간(년/월) 유효성을 확인. 부적합 시 ES_RETURN(E)로 조기 종료시킨다. | ZTPAC_CONFIG, ZTPAC_SCH_CONFIG | ES_RETURN(013/014/092) |
